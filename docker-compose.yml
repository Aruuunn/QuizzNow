version: "3.8"

services:
  db:
    restart: always
    image: "postgres:latest"
    environment: 
      - POSTGRES_PASSWORD=postgres_password
      - POSTGRES_DB=QIF
    ports: 
        - "5432:5432"

  sessionStore:
    image: "redis:latest"
    restart: always
    command: --port 8000


  api:
    stdin_open: true
    tty: true
    build:
      dockerfile: Dockerfile.dev
      context: ./server
    volumes:
      - ./server:/app
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres_password
      - POSTGRES_DATABASE=QIF
      - REDIS_SESSION_STORE_HOST=sessionStore
      - REDIS_SESSION_STORE_PORT=8000
    ports: 
        - "5000:5000"
